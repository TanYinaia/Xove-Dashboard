# Agent Dashboard 代码审查报告（通俗版）

> 审查时间：2026-07-22
> 审查范围：`src/` 全部 TypeScript 源码（10 个文件，约 4200 行）
> 审查版本：v0.2.3
> 构建状态：`npm run build` 通过（说明代码能正常编译，问题都出在「跑起来之后的逻辑」上）

## 怎么看这份报告

- 🔴 **严重问题**：会影响数据正确性或直接报错，建议优先修
- 🟡 **建议修复**：目前能凑合用，但容易出毛病或拖慢速度
- 💭 **轻微项**：小瑕疵，顺手清理就行

每条都分三块：**「在哪」**（代码位置）→ **「怎么了」**（技术描述）→ **「通俗说就是」**（生活化解释，让你秒懂后果）。

---

## 🔴 严重问题（建议优先修）

### 1. 每日节点：只填备注没点完成，却被记成「已完成」

**在哪**：`TaskEditModal.ts` 第 129 行 + `taskParser.ts` 第 210 行

**怎么了**：在「任务详情」弹窗里，如果你给某天填了备注文字，但**没有点**「今日完成 / 今日不做」按钮就直接保存，系统会把当天节点的状态记成 `todo`（待打卡）。可保存到文件时，代码只判断「是不是 skip（今日不做）」，不是 skip 的一律写成「✅ 完成」。`todo` 不是 skip，于是被当成了完成。

**通俗说就是**：就像你只在签到表上写了句「今天忙，回头补」，没勾「已完成」，但系统自动替你打了个勾——记录说今天完成了。结果这个任务当天会从待办列表里消失，你以为做完了其实没做。

---

### 2. 完成时间「记的和存的」对不上

**在哪**：`TaskEditModal.ts` 第 197 行

**怎么了**：当你打开一个**已经完成**的任务，改了点别的（比如改备注、改日期）再保存时，内存里那个「完成时间」会被刷新成「当前时刻」，但文件里的完成时间纹丝不动（代码只在「从没完成→刚完成」时才往文件里写完成时间）。于是内存和文件俩版本不一致。

**通俗说就是**：好比账本上记着「7月1日完成」，你只是去改个备注，结果电脑里临时记成了「7月22日完成」，但实际存档还是7月1日。后面凡是拿这个完成时间算日期的地方（比如节点轴画到哪天结束），就会算错。

---

### 3. 任务名带特殊字符时创建失败

**在哪**：`DashboardView.ts` 的 `createTaskFile`（约第 2470 行）

**怎么了**：新建任务时，任务名直接拿去当文件名，没清洗。如果名字里有 Windows / Obsidian 不让用的字符（`* " \ / < > : | ?`），创建文件会报错。讽刺的是，**建项目**的地方做了清洗，**建任务**的地方忘了做，两边不一致。

**通俗说就是**：你给任务起名叫「需求v1.0/初稿」，那个斜杠 `/` 电脑会当成「文件夹分隔符」，结果它以为你要建个文件夹，创建就失败了。而且报错可能只在后台，你看不到明确提示，任务就像「没建成」一样消失了。

---

### 4. 倒计时卡片显示的是假数据，永远不变

**在哪**：`DashboardView.ts` 的 `renderCountdown`（约第 3218 行）

**怎么了**：主页右下角那个「倒计时」卡片——剩余天数、剩余周数、完成百分比、里程碑——全部读自一份写死的示例数据（`MOCK_DATA`），里面固定写着「剩余 186 天 / 27 周 / 49.2%」。这些数字不随真实日期变化。

**通俗说就是**：相当于墙上挂了张印死的日历，永远写着「还剩 186 天」。不管今天是 7 月还是 12 月，它都这么显示。这是用户一眼能看到的核心信息，会直接误导你。

---

### 5. 有个文件是「死代码」，没用还占地方

**在哪**：`src/views/ProjectOverviewView.ts`（整个文件，387 行）

**怎么了**：这个文件定义了「项目总览」视图，用的是一坨写死的假数据（假项目、假任务）。但它**从来没被任何代码引用过**，也没在插件入口注册——真正的项目总览功能其实全部写在 `DashboardView.ts` 里。可它还是会被打包进最终插件，增加体积，还会让后来读代码的人以为「项目总览的入口在这儿」，实际完全不是。

**通俗说就是**：仓库角落堆了一箱贴着「项目总览」标签的旧货，但从没拆封过、也没人用，还占了地方，更会误导新来的人去翻它。直接扔掉就行。

---

## 🟡 建议修复（能凑合用，但该修）

### 6. 读取笔记标签的工具太「笨」

**在哪**：`taskParser.ts` 的 `parseFrontmatter`（第 87 行）

**怎么了**：自己手写了个简易的 YAML 解析器，但它认不出复杂格式——比如带引号的值不会去引号、数组 `[a, b]`（没加引号）解析失败、多行内容、嵌套对象统统不支持。

**通俗说就是**：就像用一把只能切直线的刀去雕花。Obsidian 其实自带了「官方解析器」（`parseYaml`），免费、标准、现成的，换上它就稳了。目前能跑是因为你的笔记格式都比较简单，一旦格式稍复杂就会读错。

---

### 7. 状态/优先级填错了不报错，但显示会乱

**在哪**：`taskParser.ts` 第 130、137、144 行

**怎么了**：读任务的状态、优先级、类型时，直接把字符串「当作」合法值用，没校验是不是在规定选项里。如果手写笔记时不小心把「已完成」写成「完成」（漏了「已」字），系统不会报错，但后续的状态颜色、看板分列、筛选全都会找不到对应项，显示就乱了。

**通俗说就是**：填表时填了个错别字，系统不提醒，直接把错别字原样显示出来，颜色、分类全对不上号。应该加个「校对员」——发现不在选项里就退回默认值。

---

### 8. 拖拽排序时位置会差一格

**在哪**：`DashboardView.ts` 第 1121 行（侧边栏项目拖拽）

**怎么了**：把一个项目从上面拖到下面时，代码先把它从原位置删掉，再插到目标位置。但删掉之后，后面的项目都往前挪了一格，这时候还用「原来的目标序号」插入，就会插到比预期靠后一格的地方。

**通俗说就是**：排队时你从第 1 个位置挪到第 3 个位置，但你一走，后面的人自动往前补了，结果你插进第 3 位时其实站到了第 4 人的后面。往**下**拖会差一格，往上拖没事。这个错误顺序还会被存下来。

---

### 9. 日期在不同时区可能差一天

**在哪**：多处日期处理

**怎么了**：代码里两种写日期的方式混用——一种 `new Date('2026-07-22')` 会被当成「伦敦时间午夜」，换成北京时间可能还是 22 号，但换成西半球时区就可能变成 21 号；另一种加了 `T00:00:00` 就是「本地时间」，没问题。两种混着用，潜在风险。

**通俗说就是**：同一张火车票，有人按北京时间看是 22 号，有人按纽约时间看成了 21 号。你在中国用暂时没影响，但这是颗定时炸弹，建议统一成「本地时间」写法。

---

### 10. 找字段时可能「找错地方」

**在哪**：`TaskEditModal.ts` 第 120 行、`DashboardView.ts` 多处

**怎么了**：判断「这个任务有没有优先级字段」时，是去**整个文件**里搜「优先级:」这几个字。可万一正文里也恰好写了「优先级:」这三个字，就会误以为「已经有了」而跳过插入。

**通俗说就是**：你想确认抽屉里有没有钥匙，结果在整间屋子搜「钥匙」俩字，碰巧书里也写着「钥匙」二字，你就以为钥匙在屋里了。应该只在「抽屉」（即文件顶部的属性区）里找。

---

### 11. 「今天」这个日期不会跨天更新

**在哪**：`TaskModal.ts` 第 68 行、`ProjectModal.ts` 第 27 行

**怎么了**：「今天」的日期是在程序刚加载时算了一次，存成常量。Obsidian 一直开着不关，过了零点，「今天」还是昨天的日期，弹窗里日期框默认值就错了。

**通俗说就是**：挂钟没电了，过了零点还显示昨天的日子。应该每次打开弹窗都重新看一眼现在几点。

---

### 12. 在设置页换主题，已打开的主页不会立刻变

**在哪**：`settings.ts` 第 306 行（`applyTheme`）

**怎么了**：设置页里切换深色/浅色主题，只更新「当前正显示着的主页元素」。如果此时主页没在前台（比如你正盯着设置页），切回去发现主题没变，得重新打开才生效。而主页自带的那个主题按钮倒是能即时切换——两个入口不同步。

**通俗说就是**：客厅换了灯泡的开关，但卧室的灯没跟着重新连。建议换主题后顺手把所有已打开的主页都刷新一下。

---

### 13. 背景噪点动画太费电

**在哪**：`DashboardView.ts` 的 `renderNoise`（约第 266 行）

**怎么了**：背景那层细密噪点，是每隔两帧就重新生成一张 1024×1024 的图（约 400 万个像素点）再画上去，一直不停。这是个纯装饰，却持续占 CPU 和电量。

**通俗说就是**：为了一个微微闪动的背景，电脑每秒钟在背后画几百万个小点，机器发烫、电池掉得快。可以改成「画一张小的噪点图，用 CSS 放大平铺」，或者干脆用一张静态图，效果差不多但几乎不耗电。

---

### 14. 同一批任务被重复扫描两遍

**在哪**：`DashboardView.ts` 的 `refreshAll`（约第 127 行）

**怎么了**：每次你改动 vault 里的文件，系统会先扫一遍所有任务更新数字（Pulse 那块），然后 200 毫秒后又扫一遍更新卡片。同一批数据被全量读了两遍。笔记少时没感觉，笔记多了会有点卡。

**通俗说就是**：同一个人被叫去翻两遍同一柜子的资料。建议「扫一次，结果存着，两处共用」。

---

### 15. 建多层文件夹会失败

**在哪**：`DashboardView.ts` 的 `createCaptureNote`（约第 513 行）、`createDiary`、`createProjectFolder`

**怎么了**：创建笔记存放文件夹时，用 Obsidian 的 `createFolder`，它只能建**一层**，不能递归建「套娃」文件夹。默认设置是「00 inbox/速记」两层，如果「00 inbox」这个父文件夹不存在，直接建「00 inbox/速记」就报错。

**通俗说就是**：你想把文件放进「抽屉→文件夹→档案袋」，但工具只会造档案袋，不会顺便造抽屉和文件夹。应该改成「一层一层往里建」，或用支持递归的底层方法。

---

### 16. 编辑项目时阶段下拉框可能显示空白

**在哪**：`ProjectModal.ts` 第 123 行

**怎么了**：编辑项目时，如果这个项目的阶段编号超过了当前设置的阶段数（比如阶段从 5 个减到 4 个，但某项目还停在「第 5 阶段」），下拉框找不到对应选项，就显示空白。

**通俗说就是**：菜单上只有 4 道菜，但你上次点的是第 5 道，菜单缩减后那个位置就空着了。应该自动夹一下，超过上限就退到最后一项。

---

### 17. 有个没用的设置项残留

**在哪**：`settings.ts` 第 24 行

**怎么了**：`mySetting` 这个字段是 Obsidian 插件模板自带的示例，全代码没有任何地方用到它。

**通俗说就是**：菜单上印着一道根本不做的菜，删了就行。

---

## 💭 轻微项（顺手清理）

### 18. 一处注释写错了
`BannerModal.ts` 第 30 行注释写「16:3」，但其实 v0.2.0 起封面比例已改成 16:2.5。注释过时了。

### 19. 变量名撞车
`DashboardView.ts` 第 1126 行，内层循环用的变量名 `p` 和外层重名了。不影响功能，但读起来容易混淆，改个名就好。

### 20. 「mockData」这名字有误导
`mockData.ts` 里那份数据，现在其实是被拿来当「静态文案」用的（比如标题副标题），不是用来兜底的假数据。叫 mock 容易让人误以为有回退机制。建议改名叫 `constants`（常量）。

### 21. 一个定时器没清理
`TaskEditModal.ts` 第 259 行有个 50 毫秒后让输入框获取焦点的定时器，如果弹窗在 50 毫秒内就关了，它会去操作一个已经不存在的框。影响极小，但规范上应该清理。

### 22. 一处缩进看着别扭
`TaskEditModal.ts` 第 12 行的缩进像是 tab 和空格混用了，统一一下就好。

---

## 修复顺序建议

| 先后 | 编号 | 为什么先修 |
|------|------|-----------|
| **第一批（立刻）** | 1、2、4 | 直接导致数据记错 / 显示假数据，用户能看见 |
| **第二批（尽快）** | 3、5 | 一个会创建失败，一个是没用的死代码该删 |
| **第三批（有空）** | 6、7、9、10、15 | 让程序更稳、更不容易出错 |
| **第四批（迭代）** | 8、11、12、13、14、16、17 | 体验和性能优化 |
| **第五批（顺手）** | 18–22 | 小清理，想到就做 |

---

## 做得好的地方（这些保持住）

不是只有问题，也有不少处理得漂亮的点：

- **防抖 + 卡片复用**：你频繁保存笔记时，主页不会一闪一闪地重画，因为做了「等一下再刷新」「外壳留着只换内容」的处理
- **写文件的工具考虑了换行符差异**（Windows/Mac 混用也不怕）
- **甘特图用固定宽度画时间轴**，不会因为浏览器伸缩被截断
- **提示气泡挂在对的位置**，不会被某些容器「压」到页面底部
- **标签字段从中文「标签」改英文 `tags` 时做了兼容**，老笔记的标签不会丢
- **项目总览的几个标签页是「用到才画」**，不会一打开就卡

---

*报告结束。如果决定修，告诉我从哪几个开始，我可以直接动手改代码。*
