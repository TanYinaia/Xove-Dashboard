# 项目整理报告

> 整理时间：2026-08-08　|　对应版本：v0.2.6（`personal` 分支）
> 本报告做清点、标注与执行记录。A / B / C 三类已于 **2026-08-08 删除**（D 类仅排除出 Share 副本，仍保留在原项目中）。

---

## 一、文档更新摘要

| 文件 | 处理 | 说明 |
|------|------|------|
| `README.md` | **整篇重写** | 原文是 Obsidian 官方示例插件模板（"Obsidian Sample Plugin"），与本项目毫无关系。改为真实项目说明：功能概览、数据约定、构建命令、目录结构、设计约定、分支模型 |
| `AGENTS.md` | **重写** | 版本 `0.1.0` → `0.2.6`；补最低 Obsidian 版本、源码结构、`build:js` 首选构建链路与受限环境说明、ItemView 白屏等三条踩坑规则、CSS 前缀与令牌规范、本地-only 的 git 纪律与分支模型 |
| `CLAUDE.md` | 同步 | 与 `AGENTS.md` 逐字节一致（两份是给不同 AI 工具读的同一份规范，改动需同步） |
| `PROJECT_CONTEXT.md` | **大幅更新** | 见下方明细 |
| `项目整理报告.md` | 新增 | 本文件 |

### `PROJECT_CONTEXT.md` 具体改动

1. **版本与命名**：0.2.4 → 0.2.6；显示名 `Agent Dashboard` → `Dashboard`（并注明 id / CSS class / 视图 id 保持不变）
2. **目录结构**：删除失效的 Windows 路径 `C:/My Dashboard`；补上遗漏的 `icons.ts`、`opportunityParser.ts`、`OpportunityModal.ts`、`rollup.config.mjs`、`README.md`、各类开发工具与素材目录
3. **技术栈 / 构建**：补 `npm run build:js`（rollup 纯 JS，首选）与独立 `tsc -noEmit` 类型检查；新增「运行环境（鸿蒙）」小节，说明 `npm run build` / `npm run dev` 在当前环境跑不通的原因
4. **关键文件说明表**：行数全部按实测值更新（`DashboardView.ts` ~3250 → 4178 行），新增 `icons.ts` / `opportunityParser.ts` / `OpportunityModal.ts` 三行
5. **设置项**：补 `themeSyncObsidian`、`dashboardTitle`、`poGanttStatusFilter`、`opportunityFile`、`currentOppView`；`theme` 类型修正为 `'auto' | 'dark' | 'light'`
6. **功能清单**：Header 与工具栏描述对齐现状（6 个按钮、含机会点、动态版本号）；新增整节「机会点视图」（数据存储、状态流、交互）；补图标系统、主题联动、自定义标题
7. **设计规范 / 配色**：替换为 2026-08-03 重做后的实际方案（深色保留 v0.2.5 hex 原值，浅色为 shadcn neutral oklch 中性阶），并写明浅色覆盖必须用的 `:is(...)` 选择器写法
8. **版本历史**：补齐缺失的 **v0.2.5**、**v0.2.6**，以及「未发布（personal 分支）」一节
9. **开发注意事项**：构建命令改为 `build:js`；git 纪律细化为「只用本地 git、不 push、不配 remote、禁止全量回退」；新增分支模型；新增「踩过的坑」小节（ItemView 字段名冲突、`containerEl.empty()`、`inset:0`、`overflow-x` 截断、内联样式需 `!important`、`main.js` 不可从 git 还原）

---

## 二、无用文件清单

> 共 **19 项 / 约 498 KB**。按可删信心从高到低排列。

### A. 临时 / 一次性产物（已于 2026-08-08 删除）

| 文件 | 大小 | 判定依据 |
|------|------|----------|
| `白屏排查报告.html` | 4.7 KB | 2026-08-04 白屏排查的一次性汇报页。该 bug 已在 v0.2.6 修复，根因与修复方式已写进 `PROJECT_CONTEXT.md`「踩过的坑」，报告本身无保留价值 |
| `Dashboard/` | 23 KB | 空壳目录，内部只有 `.workbuddy/scripts/` 一个**空文件夹**，无任何文件。疑似某次误建 |

### B. 重复 / 被取代（已于 2026-08-08 删除）

| 文件 | 大小 | 判定依据 |
|------|------|----------|
| `V0.2.zip` | 39 KB | 与 `V0.2/` 内容**完全相同**（`main.js` / `manifest.json` / `styles.css` 三件套） |
| `V0.2/` | 157 KB | v0.2 时期的旧构建快照。当前版本已到 0.2.6，且 git 本地仓库保有全部历史提交，旧产物无需以文件形式留存 |
| `prototype/simple-dashboard/` | 46 KB | 被 `prototype/simple-dashboard-01/` 取代（同一静态原型的旧一轮迭代，后者内容更完整）。若要保留原型历史，留 `-01` 与 `project-overview` 两份即可 |

### C. 已废弃模块（已于 2026-08-08 删除）

| 文件 | 大小 | 判定依据 |
|------|------|----------|
| `layout-editor.html` + `layout-editor.js` | 20 KB | 第一代布局编辑器，已被 `dashboard-editor.html` 完全取代 |
| `layout-designer.html` + `layout-designer.js` | 30 KB | 第二代布局设计器，同样被 `dashboard-editor.html` 取代 |
| `dashboard-layout-preview.css` | 1.5 KB | **孤儿文件**：全项目 grep 零引用（含 HTML / JS / PY / MD），属于上面两代编辑器的遗留 |
| `sketch-export.py`<br>`sketch-import.py`<br>`svg-export.py` | 23 KB | Sketch / 墨刀往返设计流的脚本。该路线已于 2026-08-07 弃用，改为本地自包含 HTML 编辑器（`dashboard-editor.html`）+ 单文件 HTML 导入 Figma（`figma-obsidian-dashboard.html`） |
| `agent-dashboard-design.sketch`<br>`agent-dashboard-design.svg` | 28 KB | 上述已弃用流程的导出物，无下游消费者 |

### D. 有效但不适合随项目分享（已排除出 Share 副本）

| 目录 | 大小 | 原因 |
|------|------|------|
| `node_modules/` | 211 MB | 依赖包，`npm install` 可完整重建 |
| `.git/` | 3.3 MB | 本地版本库，含全部提交历史，不适合以文件方式拷贝分发 |
| `.workbuddy/` | 2.3 MB | AI 协作的工作数据：每日记忆日志、界面截图、画布缓存、生成脚本，含个人内容 |
| `参考项目/` | 2.0 MB | 第三方插件源码（Project Manager v1.6.3，作者 Stepan Kropachev）+ 一份 DeepSeek 导出 HTML，属他人版权的参考资料，非本项目代码 |

### E. 明确保留（易被误判为无用，特此说明）

| 文件 | 保留理由 |
|------|----------|
| `CLAUDE.md` | 虽与 `AGENTS.md` 逐字节相同，但两者是给不同 AI 工具读的入口文件，都要在 |
| `esbuild.config.mjs` | 当前环境跑不通，但在正常桌面环境下 `npm run dev` / `npm run build` 依赖它，属官方模板标准链路 |
| `mockData.ts` | 名字像 mock，实际提供 `DashboardData` 等类型定义与 UI 骨架占位数据，被 `DashboardView` 引用 |
| `代码审查报告.md` | v0.2.3 源码审查报告，16 项问题已在 v0.2.4 修完；作为历史存档保留 |
| `插件图标/`（16 个 SVG） | 图标素材源文件。当前 `src/icons.ts` 由画布版生成，但素材仍是后续改图标的输入 |
| `token-playground.*`、`light-mode-preview.html`、`figma-obsidian-dashboard.html` | 在用的设计调试工具 |
| `main.js` | gitignore 的构建产物，但它是安装插件必需的三件套之一，Share 副本中保留以便直接安装 |

---

## 三、Share 副本

位置：`/storage/Users/currentUser/Share/My Dashboard/`
目录结构与原项目一致，仅剔除上表 A / B / C / D 四类内容（`白屏排查报告.html`、`Dashboard/`、`V0.2.zip`、`V0.2/`、`prototype/simple-dashboard/`、`layout-editor.*`、`layout-designer.*`、`dashboard-layout-preview.css`、`sketch-*.py`、`agent-dashboard-design.*`、`node_modules/`、`.git/`、`.workbuddy/`、`参考项目/`）——即保留更新后的文档、有效源码与设计工具。
